on:
  schedule:
    - cron: "8 5 * * *"
  workflow_dispatch:

jobs:
  print-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello! This ran at 12:50 PM PKT"
